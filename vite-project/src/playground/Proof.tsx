// esto es la unión entre EuclidSet y los grupos de particiones generadas; en
// cambio, cada grupo de particiones contiene alternativamente el formulario
// generador con el range [min, max], y el botón run (estado inicial) o las
// particiones generadas mediante el botón run (estado final); en particular
// Proof siempre tiene listo un nuevo grupo de particiones al final de todos los
// grupos

export function Proof(props) {
  return <></>;
}
